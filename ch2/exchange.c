#include <stdio.h>
int main()
{
  // int a, b, c;
  // a = 0;
  // b = 1;
  // c = a;
  // a = b;
  // b = c;
  // printf("%d %d\n", a, b);
  // return 0;

  swap(x, y);
}

swap(x, y);
int x;
int y;
int z;
z = x;
x = y;
y = z;

//1 0